<?php

/*


Ontwikkel een applicatie waarmee je records van een relationele database kan bekijken, toevoegen, 
    bewerken, verwijderen. Gebruik hiervoor PDO of de MySQLi functies. 
    De relationele database bevat minimaal 4 tabellen en pasalle soorten relaties toe: 
    one-to-one, one-to-many, en many-to-many.

Pas in je database tabellen zowel numerieke als string data types toe. 

Pas ook verschillende datum/tijden toe in je tabellen; deze worden volgende week 
    weer gebruik. Denk bijv aan een geboortedatum, ‘registered date’ en een ‘last login date’. 

Experimenteer met SELECT, INSERT, UPDATE, DELETE SQL queries met de 
    andere techniek dan je hierboven hebt gekozen (dus PDO of de MySQLi functies). 

Zorg er voor dat je ook prepared statements toe pas. 

Pas bij de SELECT queries ook ORDER BY, en GROUP BY toe

Experimenteer bij SELECT queries ook met AVG, COUNT, DISTINCT COUNT, MIN, 
    MAX en SUM. Mag in SQL-file gezet worden, maar ook via een PHP-script. 

Schrijf een CREATE DATABASE & CREATE TABLE SQL querie voor de database van je applicatie. 
    Mag in SQL-file gezet worden, maar ook via een PHP-script. 

Schrijf een SQL DROP query voor zowel table als schema. 

Pas PRIMARY KEY & FOREIGN KEY toe in de tabellen van je relationele database

Experimenteer met een SQL INNER JOIN , LEFT JOIN en RIGHT JOIN

Experimenteer met SQL TRANSACTIONS 
* Je bent vrij om bepaalde SQL queries in een .sql-bestand te zetten ipv php-script. 








*/
?>