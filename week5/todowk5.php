<?php

/*


Pas de header functie toe zoals in de voorbeelden van 
    https://www.w3schools.com/php/func_http_header.asp

Experimenteer met sessies; de session_start, session_destroy, session_id-functies 
    en de $_SESSION variable

Experimenteer met cookies; setcookie-& setrawcookie-functies. Roep de waarden 
    uit het cookie aan via de $_COOKIE en $_REQUEST variabelen 

Pas bij sessies of cookiees ook de serialize & unserialize-functies toe

Maak een formulier in HTML aan die met een POST-methode data stuurt naar een PHP-script

Maak een formulier in HTML aan die met een GET-methode data stuurt naar een PHP-script

Zet in een HTML-formulier 2 tekstvakken neer waar de naam een array is (Bijv. 
    <input name="FormArray[]">) en lees de waarden van het tekstvak uit in een PHP-script. 

Controleer met een HTML-formulier of je data kan uitlezen via de $_REQUEST variable. 

Pas de htmlspecialchars-functie toe in 1 van je bovenstaande PHP-scripts

Pas ook de urlencode-functie toe in een hyperlink met een variabele die afkomstig is 
    uit een HTML-formulier. Zie ook de PHP.net documentatie voor extra toelichting 

Pas in een HTML-formulier & PHP-script de filter_var-functie toe met alle Validate filters, 
    Sanitize filters en overige filters. 

Bouw een script dat een array omzet naar JSON of XML, en dit op het scherm toont

Bouw een script dat JSON-data of XML-data uitleest en toont op het scherm als normale tekst. 

Bouw een webservice voor je casus-opdracht om data te consumeren en data te posten in JSON of XML

Zoek een webservice uit die via SOAP werkt en roep deze aan via je PHP-script 

Pas een API toe van https://openweathermap.org/api, http://opendata.rdw.nl/of andere open-data API toe








*/
?>